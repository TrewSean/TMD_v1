# Netlify: build the Next.js site in web/. The Next runtime (@netlify/plugin-nextjs)
# is auto-detected for Next projects; publish dir is managed by it.
[build]
  base = "web"
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "22"
