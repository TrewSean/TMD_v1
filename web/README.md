# web (Phase 2)

Next.js site reading Supabase. Not started. See PLAN.md Phase 2.
Planned: App Router + TypeScript + Tailwind + `@supabase/supabase-js`, deployed on Netlify.
Reads `latest_observations`, `observations`, `adapter_health` with the anon key (read-only RLS).
